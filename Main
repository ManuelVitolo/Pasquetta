import java.util.Scanner;
public class Main {
    public static Partecipante creaPartecipante(){
        Scanner sc=new Scanner(System.in);
        System.out.println("Dai il nome, il cognome");
        String nome=sc.nextLine();
        String cognome=sc.nextLine();
        return new Partecipante(nome,cognome);
    }
    
    public static Prodotto creaProdotto(){
        Scanner sc=new Scanner(System.in);
        System.out.println("Dai il nome, la quantità e il costo del prodotto");
        String nome=sc.nextLine();
        int quantita=sc.nextInt();
        int costo=sc.nextInt();
        return new Prodotto(nome,quantita, costo);
    }
    
    public static void addProdottiInPartecipante(Prodotto f){
        Scanner sc=new Scanner(System.in);
        int caso=-1;
        do{
            System.out.println("Digita il cognome del partecipante nel quale aggiungere il prodotto");
            String nomePartecipante=sc.nextLine();
            if(nomePartecipante==lista[i].getCognome()){
                System.out.println("Crea prodotto");
                   getProdotto().aggiungiProdotto(creaProdotto());
                   
            }else{
                System.out.println("Cognome non trovato");
            }
        }while(caso==-1);       
    }
    public static void main(String args[]){
        Scanner sc=new Scanner(System.in);
        int scelta;
        do{
            System.out.println("Premi 0 per uscire, 1 per inserire la lista della spesa, 2  per inserire i partecipanti e per ogni partecipanti inserire i prodotti che compra, 3 per stampare la lista della spesa, 4 per stampare la lista dei partecipanti con affianco scritto quanto ha speso e quanto deve avere o dare");
            scelta=sc.nextInt();
            if(scelta==1){
            }else if(scelta==2){
                addProdottiInPartecipante();
            }else if(scelta==3){
               
            }else if(scelta==4){
                
            }
        }while(scelta!=0);
    }
}
